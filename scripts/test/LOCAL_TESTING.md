# Локальное тестирование поиска лидов

## Варианты тестирования

### Вариант 1: Прямой тест API (самый быстрый) ⚡

Тестирует прямой вызов Bitrix24 API с правильными параметрами `TYPE`, `VALUES`, `ENTITY_TYPE`.

```bash
# Убедитесь, что BITRIX24_WEBHOOK_URL установлен в .env или переменных окружения
node scripts/test/test-api-direct.mjs +15551234567
```

Или с другим номером:
```bash
node scripts/test/test-api-direct.mjs 15551234567
```

**Преимущества:**
- Не требует сборки проекта
- Быстро проверяет, что API принимает правильный формат
- Показывает точные параметры запроса

---

### Вариант 2: Тест через Bitrix24Client (рекомендуется) ✅

Тестирует исправленный метод `searchCRMByPhone` из нашего клиента.

```bash
# 1. Соберите проект
npm run build

# 2. Запустите тест
node scripts/test/test-search-crm-fixed.mjs +15551234567
```

Или с email:
```bash
node scripts/test/test-search-crm-fixed.mjs +15551234567 test@example.com
```

**Преимущества:**
- Тестирует реальный код, который используется в MCP сервере
- Проверяет всю цепочку: клиент → API → обработка результатов

---

### Вариант 3: Docker Compose 🐳

Запускает MCP сервер в Docker и позволяет делать запросы через HTTP.

```bash
# 1. Убедитесь, что .env файл содержит BITRIX24_WEBHOOK_URL
# 2. Соберите и запустите
docker-compose up --build

# 3. В другом терминале сделайте запрос
curl -X POST http://localhost:3000/tools/call \
  -H "Content-Type: application/json" \
  -d '{
    "name": "bitrix24_search_crm",
    "arguments": {
      "query": "+15551234567",
      "entityTypes": ["lead"]
    }
  }'
```

Или используйте HTTP клиент (Postman, Insomnia, etc.):
- URL: `http://localhost:3000/tools/call`
- Method: `POST`
- Body (JSON):
```json
{
  "name": "bitrix24_search_crm",
  "arguments": {
    "query": "+15551234567",
    "entityTypes": ["lead"]
  }
}
```

---

### Вариант 4: Через Node.js напрямую (для разработки) 🔧

Запускает MCP сервер локально через Node.js.

```bash
# 1. Соберите проект
npm run build

# 2. Запустите сервер
npm start

# Или для HTTP режима:
npm run start:http
```

Затем используйте MCP клиент или делайте HTTP запросы как в варианте 3.

---

## Переменные окружения

Убедитесь, что у вас установлена переменная:

```bash
export BITRIX24_WEBHOOK_URL="https://your-domain.bitrix24.ru/rest/USER_ID/WEBHOOK/"
```

Или создайте файл `.env` в корне проекта:
```
BITRIX24_WEBHOOK_URL=https://your-domain.bitrix24.ru/rest/USER_ID/WEBHOOK/
```

---

## Что проверяют тесты

1. ✅ Правильный формат параметров (`TYPE`, `VALUES`, `ENTITY_TYPE`)
2. ✅ Корректная отправка массива `VALUES[0]`, `VALUES[1]`, etc.
3. ✅ Обработка ответа от API
4. ✅ Получение деталей найденных лидов

---

## Устранение проблем

### Ошибка: "Communication values is not defined"
- ✅ **Исправлено!** Теперь используются параметры `TYPE`, `VALUES`, `ENTITY_TYPE` вместо `type`, `values`, `entity_type`

### Ошибка: "Cannot find module 'build/bitrix24/client.js'"
- Запустите `npm run build` перед тестом

### Ошибка: "HTTP 400: Bad Request"
- Проверьте формат номера телефона
- Убедитесь, что `BITRIX24_WEBHOOK_URL` правильный
- Проверьте права доступа вебхука в Bitrix24

---

## Рекомендуемый порядок тестирования

1. **Сначала**: Вариант 1 (прямой API тест) - быстро проверит формат
2. **Затем**: Вариант 2 (через Bitrix24Client) - проверит реальный код
3. **При необходимости**: Вариант 3 или 4 - полное тестирование MCP сервера

