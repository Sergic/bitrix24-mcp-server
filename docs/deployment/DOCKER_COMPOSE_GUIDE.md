# Bitrix24 MCP Server - Docker Compose Guide

Руководство по запуску Bitrix24 MCP Server через Docker Compose на Linux сервере.

## 📋 Требования

- Docker >= 20.10
- Docker Compose >= 2.0
- Linux сервер с доступом к интернету

## 🚀 Быстрый старт

### 1. Подготовка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# ОБЯЗАТЕЛЬНО: URL вебхука Bitrix24
BITRIX24_WEBHOOK_URL=https://your-domain.bitrix24.com/rest/USER_ID/WEBHOOK_CODE/

# Опционально: порт (по умолчанию 3000)
PORT=3000

# Опционально: адрес привязки порта (по умолчанию 127.0.0.1)
# Используйте 127.0.0.1 для привязки только к localhost (рекомендуется при использовании nginx)
# Используйте 0.0.0.0 для привязки ко всем интерфейсам (для прямого доступа извне)
BIND_ADDRESS=127.0.0.1

# Опционально: окружение (по умолчанию production)
NODE_ENV=production

# Опционально: уровень логирования (по умолчанию info)
LOG_LEVEL=info
```

**Как получить Bitrix24 Webhook URL:**
1. Войдите в ваш Bitrix24
2. Перейдите в **Приложения** → **Вебхуки**
3. Создайте **Входящий вебхук**
4. Скопируйте URL (формат: `https://domain.bitrix24.com/rest/USER_ID/WEBHOOK_CODE/`)
5. Убедитесь, что вебхук имеет права доступа к CRM и Задачам

### 2. Запуск через Docker Compose

```bash
# Сборка и запуск контейнера
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart
```

### 3. Проверка работы

После запуска проверьте:

```bash
# Проверка статуса контейнера
docker-compose ps

# Проверка health check
curl http://localhost:3000/health

# Проверка статусной страницы
curl http://localhost:3000/

# Тест MCP endpoint
curl -X POST http://localhost:3000/mcp \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "id": 1,
    "method": "tools/list"
  }'
```

## 🔧 Конфигурация

### Изменение порта

Если нужно изменить порт (например, на 8080):

1. В `.env` файле:
```bash
PORT=8080
```

2. В `docker-compose.yml` порт уже настроен автоматически через переменную `${PORT:-3000}`

3. Перезапустите:
```bash
docker-compose down
docker-compose up -d
```

### Настройка адреса привязки порта

По умолчанию порт привязан только к `127.0.0.1` (localhost) для безопасности. Это рекомендуется при использовании nginx или другого reverse proxy.

**Варианты настройки:**

1. **Только localhost (рекомендуется для nginx):**
```bash
# В .env файле
BIND_ADDRESS=127.0.0.1
PORT=3000
```
Порт будет доступен только на localhost. Используйте этот вариант, если проксируете через nginx.

2. **Все интерфейсы (для прямого доступа извне):**
```bash
# В .env файле
BIND_ADDRESS=0.0.0.0
PORT=3000
```
Порт будет доступен на всех сетевых интерфейсах. **Внимание:** Используйте только если понимаете риски безопасности. Рекомендуется использовать firewall и HTTPS.

После изменения `BIND_ADDRESS` перезапустите контейнер:
```bash
docker-compose down
docker-compose up -d
```

### Настройка логирования

Для детального логирования установите в `.env`:
```bash
LOG_LEVEL=debug
NODE_ENV=development
```

### Использование внешнего .env файла

Docker Compose автоматически читает `.env` файл из текущей директории. Если хотите использовать другой файл:

```bash
docker-compose --env-file /path/to/your/.env up -d
```

## 📊 Мониторинг

### Просмотр логов в реальном времени
```bash
docker-compose logs -f bitrix24-mcp-server
```

### Просмотр последних 100 строк логов
```bash
docker-compose logs --tail=100 bitrix24-mcp-server
```

### Проверка использования ресурсов
```bash
docker stats bitrix24-mcp-server
```

### Health Check
Контейнер автоматически проверяет здоровье сервера каждые 30 секунд:
```bash
# Проверка статуса health check
docker inspect --format='{{.State.Health.Status}}' bitrix24-mcp-server
```

## 🔄 Обновление

### Обновление образа после изменений в коде

```bash
# Остановка текущего контейнера
docker-compose down

# Пересборка образа
docker-compose build --no-cache

# Запуск нового контейнера
docker-compose up -d
```

### Обновление только переменных окружения

```bash
# Измените .env файл
# Затем перезапустите контейнер
docker-compose down
docker-compose up -d
```

## 🐛 Troubleshooting

### Контейнер не запускается

1. **Проверьте логи:**
```bash
docker-compose logs bitrix24-mcp-server
```

2. **Проверьте переменные окружения:**
```bash
docker-compose config
```

3. **Проверьте, что BITRIX24_WEBHOOK_URL установлен:**
```bash
docker-compose exec bitrix24-mcp-server env | grep BITRIX24
```

### Порт уже занят

Если порт 3000 занят, измените в `.env`:
```bash
PORT=3001
```

Или освободите порт:
```bash
# Найти процесс, использующий порт
sudo lsof -i :3000

# Остановить процесс
sudo kill -9 <PID>
```

### Проблемы с вебхуком

1. **Проверьте URL вебхука:**
```bash
curl -X GET "${BITRIX24_WEBHOOK_URL}app.info"
```

2. **Проверьте права доступа вебхука в Bitrix24**

3. **Проверьте логи контейнера на ошибки API**

### Контейнер постоянно перезапускается

1. **Проверьте логи:**
```bash
docker-compose logs --tail=50 bitrix24-mcp-server
```

2. **Проверьте health check:**
```bash
docker inspect bitrix24-mcp-server | grep -A 10 Health
```

3. **Проверьте, что все обязательные переменные установлены**

## 🔒 Безопасность

### Рекомендации для продакшена

1. **Не коммитьте `.env` файл в git:**
```bash
# Убедитесь, что .env в .gitignore
echo ".env" >> .gitignore
```

2. **Используйте секреты Docker Compose (для Docker Swarm):**
```yaml
secrets:
  webhook_url:
    external: true
```

3. **Ограничьте доступ к порту:**
   - Используйте `BIND_ADDRESS=127.0.0.1` для привязки только к localhost
   - Используйте firewall
   - Настройте reverse proxy (nginx, traefik)
   - Используйте HTTPS через reverse proxy

4. **Регулярно обновляйте вебхук URL** для безопасности

## 🌐 Интеграция с Reverse Proxy

### Пример с Nginx

**Важно:** Убедитесь, что в `.env` установлено `BIND_ADDRESS=127.0.0.1`, чтобы порт был доступен только на localhost.

```nginx
server {
    listen 80;
    server_name mcp.yourdomain.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### Пример с Traefik (docker-compose)

Добавьте labels к сервису:
```yaml
services:
  bitrix24-mcp-server:
    # ... существующая конфигурация ...
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bitrix24.rule=Host(`mcp.yourdomain.com`)"
      - "traefik.http.routers.bitrix24.entrypoints=websecure"
      - "traefik.http.routers.bitrix24.tls.certresolver=letsencrypt"
```

## 📝 Полезные команды

```bash
# Просмотр конфигурации
docker-compose config

# Выполнение команды внутри контейнера
docker-compose exec bitrix24-mcp-server sh

# Очистка (удаление контейнера и сети)
docker-compose down

# Очистка с удалением volumes
docker-compose down -v

# Пересборка без кеша
docker-compose build --no-cache

# Запуск в foreground режиме (для отладки)
docker-compose up

# Остановка всех сервисов
docker-compose stop

# Запуск остановленных сервисов
docker-compose start
```

## 📚 Дополнительная информация

- [HTTP Streamable Guide](HTTP_STREAMABLE_GUIDE.md) - Подробности о HTTP транспорте
- [README.md](README.md) - Основная документация
- [QUICK_START_HTTP.md](QUICK_START_HTTP.md) - Быстрый старт без Docker

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs -f`
2. Проверьте health check: `curl http://localhost:3000/health`
3. Проверьте переменные окружения: `docker-compose config`
4. Откройте issue на GitHub

